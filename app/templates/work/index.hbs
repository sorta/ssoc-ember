<h4>work index</h4>

<div class="container">
  {{#each @model as |manager|}}
    <div class="text-center mb-8">
      <LinkTo @route="work.manager" @model={{manager.id}}>{{manager.name}}:</LinkTo>
      {{manager.description}}
    </div>
  {{/each}}
</div>